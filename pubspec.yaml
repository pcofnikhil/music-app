name: flutter_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  just_audio: ^0.9.36
  file_picker: ^8.0.0
  permission_handler: ^11.3.0
  provider: ^6.1.1
  path_provider: ^2.1.2
  metadata_god: ^0.5.2+1
  path: ^1.8.3  # ✅ Added missing dependency

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependency_overrides:
  file_picker: ^8.0.0  # ✅ Overriding to avoid platform issues

flutter:
  uses-material-design: true
